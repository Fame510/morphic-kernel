# Port the kernel listens on
PORT=3001
# Port for the optional WASM runtime kernel
WASM_PORT=3002
# Owner key used to bind synthesized modules and seed deterministic tie-breaks
OWNER_KEY=replace_me_with_a_random_secret
# Set to 1 for verbose registry event logging
MORPHIC_DEBUG=0
